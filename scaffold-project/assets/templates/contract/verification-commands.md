# Verification Commands

検証コマンドのsource of truthとして使う。タスクごとの実行予定、実行結果、未実行理由は `docs/work/<task-id>.state.json` の `commands` へ残す。

## Default Commands

- build:
- test:
- lint:
- typecheck:

## Command Notes

- needs network:
- needs secret:
- needs GUI / dev server:
- timeout:
- artifacts:

## Human Decisions

- 要確認
