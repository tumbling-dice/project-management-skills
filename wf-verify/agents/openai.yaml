interface:
  display_name: "Workflow: Verify"
  short_description: "検証結果を証跡として整理する"
  default_prompt: "$wf-verify を使って、承認済み計画、差分、変更ファイル、repo手順から検証範囲を確定し、repo-local test_runnerへ委譲してwf-review用の証跡を作ってください。修正やreview判定はしないでください。"

policy:
  allow_implicit_invocation: false
