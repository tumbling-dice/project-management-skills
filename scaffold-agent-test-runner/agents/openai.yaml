interface:
  display_name: "scaffold-agent-test-runner"
  short_description: "repo内の test_runner agent 定義を作る"
  default_prompt: "$scaffold-agent-test-runner を使って、このrepoに検証専用の test_runner custom agent とrepo固有の検証手順を提案してください。"

policy:
  allow_implicit_invocation: true
