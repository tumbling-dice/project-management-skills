interface:
  display_name: "idiot"
  short_description: "未決判断を人間向けの質問へ整理する"
  default_prompt: "$idiot を使って、既存成果物の未確認事項から進行を止める人間判断だけを質問化してください。各質問に選択肢、根拠つき推奨値、保留時の戻り先を付けてください。"

policy:
  allow_implicit_invocation: true
