interface:
  display_name: "scaffold-agent-prep-scout"
  short_description: "wf-explore用read-only prep scoutを設計する"
  default_prompt: "$scaffold-agent-prep-scout を使って、wf-explore用のread-only prep scout、repo-local委譲契約、work-contextのevidence欄を設計してください。実装計画やreview、検証は行わないでください。"

policy:
  allow_implicit_invocation: true
