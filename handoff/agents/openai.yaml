interface:
  display_name: "handoff"
  short_description: "作業成果物を次のAIセッション用packetに整理する"
  default_prompt: "$handoff を使って、現在の成果物を次のAIセッションが読めるhandoff packetに整理してください。"

policy:
  allow_implicit_invocation: false
