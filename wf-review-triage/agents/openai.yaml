interface:
  display_name: "wf-review-triage"
  short_description: "review指摘を修正・再計画・調査・人間判断へ分類する"
  default_prompt: "$wf-review-triage を使って、review指摘を承認済み計画内で修正できるもの、再検証、再計画、追加調査、人間判断へ戻すものに分類してください。"

policy:
  allow_implicit_invocation: false
